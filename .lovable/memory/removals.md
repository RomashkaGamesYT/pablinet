Registry of removed features and rejected ideas

- DM/Messages: Removed from navigation, routes, and settings. MessagesPage still exists as file but not routed.
- Settings page: Merged into Profile page as a tab (no separate /settings route).
